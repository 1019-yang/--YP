# 杨鹏 · 电商简历与作品集网站交接文档

> 本文件写给后续接手的智能体（WorkBuddy、Codex 等）。读完本文后，应能直接继续维护、更新和交付以下内容，无需重新摸索环境。
>
> 最后更新：2026-08-19
> 覆盖范围：电商简历（HTML/PDF）、线上作品集网站（GitHub Pages）、视频素材与封面制作、上线发布。

## 1. 项目总览

本项目为候选人杨鹏维护求职交付物，目标岗位方向为“电商短视频编导 / 新媒体运营 / 千川信息流内容”。

| 交付物 | 位置 | 当前状态 |
| --- | --- | --- |
| 电商编导简历（两页版） | `E:\求职简历优化\tailored\电商编导\resume_visual.html` / `.pdf` | 已交付，两页、无日期 |
| 简历事实档案 | `E:\求职简历优化\简历数据\resume-facts.yaml` | facts_version 3，唯一事实源 |
| 线上作品集网站 | `E:\网站` | 已上线，工作台看板风格 |
| 网站正式链接 | `https://1019-yang.github.io/--YP/` | 线上可访问 |
| 网站代码仓库 | `https://github.com/1019-yang/--YP.git` | main 分支 |
| 原始项目数据 | `F:\交接归纳\...` | 只读数据源，F 盘可能不稳定 |
| 视频封面/帧素材 | `E:\AI小说\video-work*` | 用于截帧、压缩、封面制作 |

## 2. 工作区与关键文件

### 2.1 简历工作区 `E:\求职简历优化`

```text
E:\求职简历优化\
├── 简历数据\resume-facts.yaml                 # 唯一事实源，先读这里
├── 杨鹏_编导_视觉创意版.html                  # 母版，不要覆盖
├── 杨鹏_编导_视觉创意版.pdf
├── 杨鹏_编导_视觉创意版.resume-manifest.json
├── node_modules                               # 指向 bundled runtime 的 junction，勿递归删除
├── .measure-resume\
│   ├── render_resume.mjs                      # 用 Playwright/Edge 渲染 PDF
│   ├── validate_resume.mjs                    # 两页版溢出/图片校验
│   └── extract_poster.mjs                     # 从视频截取作品集封面
│   ├── apply-manual-site.cjs                  # 网站接入学校活动手册图的脚本
│   └── verify-manual-site.cjs                 # 网站本地渲染验证/截图
├── 电商简历项目交接.md                         # 本文档
└── tailored\电商编导\
    ├── resume_visual.html                     # 最新两页版 HTML
    ├── resume_visual.pdf                      # 最新两页版 PDF
    ├── resume_visual.resume-manifest.json
    ├── resume_visual_preview-1.png            # 第一页预览
    ├── resume_visual_preview-2.png            # 第二页预览
    ├── matching-analysis.md                   # 岗位匹配分析
    ├── version-notes.md                       # 版本记录
    └── assets\portfolio\
        ├── video_poster.jpg                   # 已接入作品集：蕉内 502COOLAIR 千川成片封面
        └── 1.png ... 8.png                    # 学校活动手册素材，已备好、尚未接入 HTML
```

### 2.2 作品集网站 `E:\网站`

```text
E:\网站\
├── index.html          # 页面结构；CSS/JS 引用带 ?v= 版本号
├── styles.css          # 工作台看板风格，暗色默认
├── main.js             # 数据渲染、作品分组、导航、灯箱等交互
├── data.js             # 全部个人资料、经历、作品、账号数据
├── README.md           # 本地维护说明
├── .gitignore          # 已忽略 mockups/previews/脚本/.tools
├── assets\
│   ├── avatars\        # 运营账号头像
│   └── videos\         # 已压缩网页版视频 + 封面 JPG
└── .tools\impeccable\  # 外部设计 QA 工具（可选参考）
```

### 2.3 视频素材工作区 `E:\AI小说`

```text
E:\AI小说\
├── video-work\                        # 通用截帧/封面脚本与素材
├── video-work-supor\                  # 苏泊尔纯钛炒锅帧素材 + make_supor_covers.py
├── video-work-jianei2\                # 蕉内 300s 睡裙帧素材 + make_jianei2_covers.py
└── video-work-huaxizi-sunscreen\      # 花西子小黛伞防晒帧素材
```

原始视频一般在 `F:\交接归纳\成果文档\` 下（康巴赫合集、苏泊尔刀具、苏泊尔炒锅等），用户也可能直接提供新路径。F 盘是外部/网络盘，读取和复制时要即时校验。

## 3. 当前状态快照

### 3.1 简历

- 岗位定位：电商短视频编导。
- 版式：`creative-bold` 两页版，页面内不出现任何日期。
- 内容主线：一饼创意（康巴赫/苏泊尔/蕉内千川全流程，脚本 40+、拍摄 30+、近 30 天 85 条、整体 ROI 2.92、GMV 2.47 万、千川分 80%、8 大爆款钩子）；爱和健康新媒体运营；长江日报全媒体运营；江汉大学教育/在校经历。
- 作品集：已接入 `video_poster.jpg`（蕉内 502COOLAIR 千川产品展示成片封面）；`assets\portfolio\1.png ... 8.png` 已复制到本地，尚未写入 HTML。
- 数据口径：所有数字必须能在 `resume-facts.yaml` 或原始文档中追溯；“年度 ROI 3.2 / 307 万+”是苏泊尔项目级数据，不是个人独立指标。

### 3.2 作品集网站

- 风格：暗色工作台看板，金色点缀；侧栏导航：概览 / 作品 / 经历 / 项目 / 联系我。
- 首屏：`YANG PENG`、形象照、口号“将重复的事，做成值得留下的作品。”。
- 个人档案：姓名杨鹏，侧栏简介“一拳打爆地球的一拳超人”，bio 为新媒体运营定位。
- 视觉替换：侧栏 `YP` 图标已替换为马克笔涂鸦 Q 版少年（`assets/yp-logo.jpg`）；首页形象照已替换为微信相册图片（`assets/hero-portrait.jpg`，网页压缩版）。
- 联系方式：电话 `13628688348`、邮箱 `1019073032@qq.com`；微信、坐标仍为“待补充”。
- 作品分组及数量：
  - 高质视频：15 支（蕉内 502COOLAIR 3、苏泊尔纯钛炒锅 7、蕉内 300s 睡裙 3、花西子小黛伞 2）。
  - 信息流视频：6 支（防烫蒸锅、雪平锅、乌金檀木菜板、0 涂层煎锅、苏泊尔鲨鱼齿刀具、纯钛炒锅 2 代）。
  - 学生时期作品：6 支视频（美团纪录片、逃离乌托邦预告片、雅客棒棒糖、长江日报采访×2、长江日报研学）+ 8 张桃花诗歌会活动手册图。
- 展示规则：
  - 高质视频按 `viralGroup` 分组为“爆款视频迭代 1/2/3”。
  - 信息流视频为两排网格，不横向滑动。
  - 学生时期作品分横屏、竖屏两排，并展示“运营账号”卡片（3 个抖音账号）。
  - 学生时期作品竖屏视频下方有“学校活动手册制作”横向卡片，8 张手册图按 01-08 顺序展示。
- 项目页：新增“AI 辅助千川脚本量产”案例，采用“结论指标 + 量产工作流 + 可轮换维度体系 + 迭代时间轴 + 落地成片”的 A+C 呈现；数据来自 `resume-facts.yaml`，`F:\交接归纳\信息流脚本大师V2.md` 是辅助创作 Skill，不是主项目。
- 当前 Git 状态：`E:\网站` 本地与线上 main 已同步，HEAD：`5f9ede0`（chunk 4: portfolio page updates and handoff doc）。
- 最新线上已确认版本为 workbench 看板版，包含花西子替换、活动手册图、联系方式调整与“AI 辅助千川脚本量产”项目案例。

### 3.3 待确认/未完成

- 网站：最近一次“第 4 个视频替换为花西子小黛伞防晒”已上线。
- 网站：8 张桃花诗歌会活动手册图已接入“学生时期作品”，位于竖屏视频下方，小标题“学校活动手册制作”；已上线。
- 网站：项目页新增“AI 辅助千川脚本量产”案例，已上线。
- 简历：用户已确认暂不处理简历，8 张素材仍保留在 `tailored\电商编导\assets\portfolio\`，未写入简历 HTML。
- 简历：是否把线上作品集链接/二维码加入简历，需与用户确认。
- 网站：微信、坐标仍为“待补充”。

## 4. 事实档案规范

简历内容必须来自 `E:\求职简历优化\简历数据\resume-facts.yaml`：

- 只写 `confidence: high` 且 `metric_status: verified` 的 claim。
- 新增事实必须登记 claim，字段含：`claim_id`、`section`、`claim`、`source`、`confidence`、`evidence`、`metric_status`。
- 待确认内容只能写进 `matching-analysis.md` 或 `pending_claims`，不能进简历正文。
- 用户提供的新文本视为 `source: user`，可登记为 high。
- 每次更新后递增 `facts_version`。

原始数据文档（只读）：

```text
F:\交接归纳\苏泊尔2025内容复盘.pdf
F:\交接归纳\康巴赫6月复盘.pdf
F:\交接归纳\大官旗-一饼近30天视频数据 6.15.xlsx
```

## 5. 所需 Skills

Codex 接手时应先读取对应 `SKILL.md`；WorkBuddy 等智能体如果不直接支持 Codex skills，也可把下述 SKILL.md 当作工作说明阅读，关键步骤已在本文件第 6-9 节写清。

### 5.1 简历核心

| Skill | 路径 | 用途 |
| --- | --- | --- |
| resume-builder | `C:\Users\10190\.codex\skills\html-resume-builder\SKILL.md`、`C:\Users\10190\.codex\skills\resume-builder\SKILL.md` | HTML 简历生成、排版、改写规范 |
| optimize-resume | `C:\Users\10190\.codex\skills\optimize-resume\SKILL.md` | 简历文字优化，不覆盖原文件 |
| jd-tailorer | `C:\Users\10190\.codex\skills\jd-tailorer\SKILL.md` | 按具体 JD 生成定制版 HTML/PDF |
| job-description-analyzer | `C:\Users\10190\.codex\skills\job-description-analyzer\SKILL.md` | JD 要求地图、匹配度分析 |
| resume-version-manager | `C:\Users\10190\.codex\skills\resume-version-manager\SKILL.md` | 母版与定制版版本管理 |
| resume-ats-optimizer | `C:\Users\10190\.codex\skills\resume-ats-optimizer\SKILL.md` | ATS 可解析性检查 |
| resume-bullet-writer | `C:\Users\10190\.codex\skills\resume-bullet-writer\SKILL.md` | 单条经历改写 |
| resume-workflow | `C:\Users\10190\.codex\skills\resume-workflow\SKILL.md` | 完整简历流程编排 |

### 5.2 网站与媒体核心

| Skill | 路径 | 用途 |
| --- | --- | --- |
| playwright | `C:\Users\10190\.codex\skills\playwright\SKILL.md` | 本地/线上页面渲染、交互验证、截图 |
| vision-assist | `C:\Users\10190\.codex\skills\vision-assist\SKILL.md` | 无法直接看图时做 OCR / 图像描述 |
| verification-before-completion | `C:\Users\10190\.codex\skills\verification-before-completion\SKILL.md` | 交付前验证 |
| control-in-app-browser | `C:\Users\10190\.codex\plugins\cache\openai-bundled\browser\26.810.52044\skills\control-in-app-browser\SKILL.md` | 在应用内浏览器打开/检查页面 |
| control-chrome | `C:\Users\10190\.codex\plugins\cache\openai-bundled\chrome\26.810.52044\skills\control-chrome\SKILL.md` | 用 Chrome 验证线上页面 |
| imagegen | `C:\Users\10190\.codex\skills\.system\imagegen\SKILL.md` | 需要生成占位/补充视觉素材时使用 |

### 5.3 文档与数据

| Skill | 路径 | 用途 |
| --- | --- | --- |
| pdf | `C:\Users\10190\.codex\plugins\cache\openai-primary-runtime\pdf\26.805.11740\skills\pdf\SKILL.md` | PDF 提取、渲染、验证 |
| spreadsheets | `C:\Users\10190\.codex\plugins\cache\openai-primary-runtime\spreadsheets\26.805.11740\skills\spreadsheets\SKILL.md` | 读取分析 xlsx 数据 |

## 6. 运行环境

不要假设系统全局 Python / Node 可用。优先使用 bundled runtime：

```text
Python: C:\Users\10190\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe
Node:   C:\Users\10190\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe
Node modules: C:\Users\10190\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\node_modules
Playwright CLI: <node_modules>\playwright\cli.js
Poppler pdftoppm: C:\Users\10190\.cache\codex-runtimes\codex-primary-runtime\dependencies\native\poppler\Library\bin\pdftoppm.exe
系统 Edge: C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe
```

关键点：

- 本机没有 ms-playwright 自带浏览器，必须用系统 Edge：`chromium.launch({ channel: "msedge", headless: true })`。
- 视频封面脚本使用 Pillow 和 Windows 字体 `msyh.ttc` / `msyhbd.ttc`。
- ffmpeg 之前会话用过，但当前不一定在 PATH 中；压缩视频前先执行 `Get-Command ffmpeg` 确认，找不到就不要写死命令。
- `E:\求职简历优化\node_modules` 是 junction，删除时用 `[System.IO.Directory]::Delete(link, $false)`，禁止 `-Recurse`。

## 7. 标准工作流

### 7.1 更新简历

1. 读取 `简历数据\resume-facts.yaml`，先更新/追加 claim，再改简历。
2. 编辑 `tailored\电商编导\resume_visual.html`。保留以下标签：

```html
data-resume-editor-template="creative-bold"
data-resume-editor-version="1"
data-resume-editor-id="yangpeng-ecommerce-short-video-director"
```

3. 渲染 PDF：

```powershell
& 'C:\Users\10190\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe' 'E:\求职简历优化\.measure-resume\render_resume.mjs'
```

4. 校验两页版（不要跑强制单页的 `validate_resume.py`）：

```powershell
& 'C:\Users\10190\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe' 'E:\求职简历优化\.measure-resume\validate_resume.mjs'
```

5. 用 pdftoppm 生成两页 PNG，检查页面边缘和内容分配。
6. 同步更新 `matching-analysis.md`、`version-notes.md`、`resume_visual.resume-manifest.json`（重新计算 SHA256）。

### 7.2 更新网站资料或作品

1. 打开 `E:\网站\data.js`，修改 `name`、`bio`、`contact`、`workGroups`、`works`、`operatedAccounts` 等字段。
2. 新视频先放 `E:\网站\assets\videos\`，并生成同名封面 JPG。
3. 在 `works` 数组新增条目，`group` 填 `high` / `feed` / `student`，`viral` 与 `viralGroup` 只在需要进入“爆款视频迭代”时填写。
4. 如果替换了封面或样式，更新 `index.html` 里的 `?v=YYYYMMDDx`，并同步更新 `data.js` 中 poster 的 `?v=` 参数，避免线上缓存。
5. 本地启动静态服务：

```powershell
& 'C:\Users\10190\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe' -m http.server 8137 --directory 'E:\网站'
```

6. 用 Playwright/Edge 打开 `http://127.0.0.1:8137/`，检查桌面和手机宽度：无报错、图片/视频可加载、按钮切换正常、无溢出。
7. 用户确认后提交推送：

```powershell
git -C 'E:\网站' add .
git -C 'E:\网站' commit -m "update portfolio"
git -C 'E:\网站' push
```

8. 线上构建完成后，用真实浏览器打开 `https://1019-yang.github.io/--YP/` 再验证一次。

### 7.3 新增/替换视频

1. 先从 F 盘或用户提供的路径确认文件存在，读取大小和分辨率。
2. 超过 100MB 的文件必须压缩；GitHub 单文件上限约 100MB。历史做法是压成网页流畅版，约 8-16MB、3.5Mbps、支持边下边播：

```text
ffmpeg -i input.mp4 -c:v libx264 -b:v 3500k -maxrate 4000k -bufsize 8000k -movflags +faststart -c:a aac -b:a 128k output.mp4
```

3. 竖屏保持 1080×1920，横屏保持原比例；4K 素材可降到 1080p。
4. 截帧时避免纯黑开头帧，选择最早有清晰画面的帧做封面。
5. 封面统一沿用现有风格（左上“视频作品”、金色装饰线、底部 `YANG PENG`），不要随意重新设计。

### 7.4 生成封面

已存在的脚本会直接输出到 `E:\网站\assets\videos\`：

```text
E:\AI小说\video-work-supor\make_supor_covers.py
E:\AI小说\video-work-jianei2\make_jianei2_covers.py
```

运行方式：

```powershell
& 'C:\Users\10190\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe' 'E:\AI小说\video-work-supor\make_supor_covers.py'
```

新品牌/新视频先看现有脚本的排版参数，再复制一套小脚本，不要直接改已有品牌脚本的固定文案。

### 7.5 验证清单

网站：

- 三个作品 tab 可切换，默认高质视频。
- 高质视频的“爆款视频迭代 1/2/3”分组正确。
- 信息流视频为两排网格；学生时期作品分横竖屏。
- 视频能播放、能打开大屏预览；封面非黑图。
- 控制台无报错、无 404；图片全部加载。

简历：

- PDF 恰为 2 页，两页文本均可提取。
- `.resume-page` 无横向/纵向溢出。
- 第一页含一饼/爱和健康，第二页含长江日报/在校经历。
- 预览 PNG 边缘无内容贴边。

## 8. 用户偏好与硬约束

- 简历不写任何日期，事实档案中可以保留。
- 简历允许两页，不再为单页删减内容。
- 不编造数据；每个数字必须能追溯。
- 原母版 `杨鹏_编导_视觉创意版.html` 不覆盖。
- 简历语言为中文，HTML 为 UTF-8。
- 网站页面不出现 “AIGC 视频作品” 这类表述，统一用“视频作品”。
- 网站已从早期苹果极简风演进为工作台看板风格，后续小步调整，不要整体推翻。
- 作品分组、横竖屏规则、封面风格以用户最近确认的版本为准；修改前先说明改动点。
- 低相关经历可压缩，但用户要求完整时优先保留。

## 9. 已知坑

- PowerShell here-string 管道中文会变成 `?`：路径和中文关键词用 base64 环境变量传递，或直接使用 `.mjs` / `.py` 脚本文件。
- `npx` 受 Windows 执行策略限制：直接用 bundled Node + playwright CLI 或自定义 mjs。
- 本机无 Playwright 自带浏览器：必须用 `channel: "msedge"`。
- F 盘可能突然断开：复制文件后立即验证，不要反复假设文件仍在。
- 视频过大或码率过高会导致网页卡顿：必须压缩并使用 `+faststart`。
- 替换封面后用户看到旧图多为缓存：更新 `?v=` 版本号并提示 `Ctrl + F5`。
- 删除 node_modules junction 时禁止 `-Recurse`。
- 当前会话的 `view_image` 可能不可用：用像素统计或边缘检查替代，并保留 PNG 给用户查看。

## 10. 当前待办

- 网站：最新本地修改（花西子替换、手册图、联系方式、项目案例）已上线。
- 网站：后续小步更新继续沿用“本地验证 → 提交 → 推送”流程。
- 简历：确认是否把线上作品集链接或二维码加入作品集区域。
- 网站：补齐微信、坐标等“待补充”字段。
- 投递具体公司 JD 时，用 `jd-tailorer` 生成 `tailored\<公司名>-<岗位>\` 定制版。

## 11. 最近一次交付状态

- 简历：两页版、无日期；facts_version 3；HTML/PDF 均通过两页校验，预览 PNG 已生成。
- 网站：workbench 看板版已上线，正式链接 `https://1019-yang.github.io/--YP/`；本地与线上同步，HEAD `5f9ede0`。
- 视频：网站当前登记 15 支高质视频、6 支信息流视频、6 支学生作品，均为网页压缩版并带封面；另已接入 8 张活动手册图，已上线。
- 网站：已新增“AI 辅助千川脚本量产”项目案例，替换侧栏图标与首页形象照，图片已加版本参数避免缓存；页面标题改为“杨鹏 作品集”，形象照亮度调高，版本号 `20260819e`，已上线。
